阅读关键代码文件，梳理执行流程，然后给出渐进式问题。


[8 tools called]


基于已看到的代码和文档，整理渐进式问题，帮你理解 NEMU 的指令执行流程。

## 渐进式理解问题

### 第一层：整体流程（从宏观到微观）

**问题1：指令执行的入口在哪里？**
- 查看 `nemu/src/cpu/cpu-exec.c` 的 `cpu_exec()` 函数
- 它调用了什么函数？这个函数做了什么？

**问题2：一条指令的执行包含哪些步骤？**
- 查看 `execute()` 函数（第75-91行）
- 它在一个循环中调用了什么函数来执行单条指令？
- 类比：就像做一道菜需要哪些步骤？

**问题3：`exec_once()` 函数做了什么？**
- 查看第44-73行的 `exec_once()` 函数
- 它接收什么参数？这些参数代表什么？
- 函数内部按顺序做了什么？

### 第二层：理解关键数据结构

**问题4：`Decode` 结构体是什么？**
- 文档提到 `Decode` 存放执行一条指令所需的信息
- 在 `exec_once()` 中，`s->pc`、`s->snpc`、`s->dnpc` 分别是什么？
- 为什么需要这三个不同的PC值？

**问题5：snpc 和 dnpc 的区别是什么？**
- 文档第208-221行解释了静态指令和动态指令
- 对于普通指令（如 `add`），snpc 和 dnpc 相同吗？
- 对于跳转指令（如 `jmp`），snpc 和 dnpc 相同吗？

### 第三层：指令周期的四个阶段

**问题6：取指（IF - Instruction Fetch）**
- 文档第61-66行说明取指的本质是什么？
- `isa_exec_once()` 如何获取指令？
- 为什么取指后要更新 `snpc`？

**问题7：译码（ID - Instruction Decode）**
- 文档第68-152行介绍了模式匹配
- `INSTPAT` 宏的作用是什么？
- 模式字符串中的 `0`、`1`、`?` 分别代表什么？
- 为什么需要 `decode_operand()` 函数？

**问题8：执行（EX - Execute）**
- 文档第198-202行说明执行阶段做什么？
- 执行阶段使用的数据来自哪里？
- 执行结果写到哪里？

**问题9：更新PC**
- 查看 `exec_once()` 第48行：`cpu.pc = s->dnpc;`
- 为什么用 `dnpc` 而不是 `snpc` 来更新PC？

### 第四层：深入理解细节

**问题10：模式匹配是如何工作的？**
- 文档第97-114行展示了宏展开后的代码
- `pattern_decode()` 函数做了什么？
- `key`、`mask`、`shift` 分别代表什么？

**问题11：为什么需要结构化程序设计？**
- 文档第244-337行讨论了代码解耦
- 如果每条指令都独立实现，会有什么问题？
- `INSTPAT` 宏如何帮助实现代码复用？

**问题12：立即数背后的字节序问题**
- 文档第157-183行讨论了字节序
- 为什么小端机和大端机需要不同的处理？
- 如果NEMU运行在大端机上，需要注意什么？

## 建议的学习路径

1. 先看代码：打开 `nemu/src/cpu/cpu-exec.c`，对照文档理解每个函数
2. 追踪一条指令：从 `cpu_exec()` → `execute()` → `exec_once()` → `isa_exec_once()`
3. 理解数据结构：找到 `Decode` 结构体的定义，理解每个字段
4. 看一个具体例子：找一个已实现的指令（如 `auipc`），追踪它的完整执行过程

## 实践练习

尝试回答这些问题，并在代码中找到对应位置：

1. 在 `cpu-exec.c` 中，`exec_once()` 函数在第几行？它调用了哪个ISA相关的函数？
2. `s->snpc` 在什么时候被更新？`s->dnpc` 在什么时候被设置？
3. 如果实现一条新指令，需要在哪个文件中添加代码？

需要我帮你查看某个具体文件或解释某个概念吗？