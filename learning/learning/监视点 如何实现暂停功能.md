查看监视点触发后程序暂停的实现机制。

程序暂停机制如下：

## 程序暂停的完整流程

### 1. 监视点触发时设置状态

在 `execute()` 函数中（第75-91行）：

```75:91:nemu/src/cpu/cpu-exec.c
static void execute(uint64_t n) {
  Decode s;
  for (;n > 0; n --) {
    exec_once(&s, cpu.pc);
    g_nr_guest_inst ++;
    trace_and_difftest(&s, cpu.pc);
    
    // 检查监视点
    if (check_wp()) {
      nemu_state.state = NEMU_STOP;  // 设置状态为停止
      break;                          // 退出循环
    }
    
    if (nemu_state.state != NEMU_RUNNING) break;
    IFDEF(CONFIG_DEVICE, device_update());
  }
}
```

关键步骤：
- 第83行：`check_wp()` 返回 `true` 表示监视点触发
- 第84行：设置 `nemu_state.state = NEMU_STOP`
- 第85行：`break` 退出 `for` 循环，停止执行指令

### 2. 状态枚举定义

状态定义在 `nemu/include/utils.h`：

```c
enum { NEMU_RUNNING, NEMU_STOP, NEMU_END, NEMU_ABORT, NEMU_QUIT };
```

- `NEMU_RUNNING`: 正在执行
- `NEMU_STOP`: 已暂停（等待用户命令）
- `NEMU_END`: 程序正常结束
- `NEMU_ABORT`: 程序异常终止
- `NEMU_QUIT`: 退出NEMU

### 3. 状态检查机制

在 `execute()` 循环中还有另一个检查（第88行）：

```c
if (nemu_state.state != NEMU_RUNNING) break;
```

如果状态不是 `NEMU_RUNNING`，也会退出循环。

### 4. cpu_exec 函数处理

`cpu_exec()` 函数（第108-136行）控制执行流程：

```108:136:nemu/src/cpu/cpu-exec.c
void cpu_exec(uint64_t n) {
  g_print_step = (n < MAX_INST_TO_PRINT);
  switch (nemu_state.state) {
    case NEMU_END: case NEMU_ABORT: case NEMU_QUIT:
      printf("Program execution has ended. To restart the program, exit NEMU and run again.\n");
      return;
    default: nemu_state.state = NEMU_RUNNING;
  }

  uint64_t timer_start = get_time();

  execute(n);

  uint64_t timer_end = get_time();
  g_timer += timer_end - timer_start;

  switch (nemu_state.state) {
    case NEMU_RUNNING: nemu_state.state = NEMU_STOP; break;

    case NEMU_END: case NEMU_ABORT:
      Log("nemu: %s at pc = " FMT_WORD,
          (nemu_state.state == NEMU_ABORT ? ANSI_FMT("ABORT", ANSI_FMT("HIT GOOD TRAP", ANSI_FG_GREEN) :
            ANSI_FMT("HIT BAD TRAP", ANSI_FG_RED))),
          nemu_state.halt_pc);
      // fall through
    case NEMU_QUIT: statistic();
  }
}
```

- 第114行：默认设置为 `NEMU_RUNNING`
- 第119行：调用 `execute(n)` 执行指令
- 第125行：如果执行完还是 `NEMU_RUNNING`，自动设置为 `NEMU_STOP`（正常执行完毕）

### 5. 主循环等待用户输入

主循环在 `sdb_mainloop()` 中（第211-247行）：

```211:247:nemu/src/monitor/sdb/sdb.c
void sdb_mainloop() {
  if (is_batch_mode) {
    cmd_c(NULL);
    return;
  }

  for (char *str; (str = rl_gets()) != NULL; ) {
    char *str_end = str + strlen(str);

    /* extract the first token as the command */
    char *cmd = strtok(str, " ");
    if (cmd == NULL) { continue; }

    /* treat the remaining string as the arguments,
     * which may need further parsing
     */
    char *args = cmd + strlen(cmd) + 1;
    if (args >= str_end) {
      args = NULL;
    }

#ifdef CONFIG_DEVICE
    extern void sdl_clear_event_queue();
    sdl_clear_event_queue();
#endif

    int i;
    for (i = 0; i < NR_CMD; i ++) {
      if (strcmp(cmd, cmd_table[i].name) == 0) {
        if (cmd_table[i].handler(args) < 0) { return; }
        break;
      }
    }

    if (i == NR_CMD) { printf("Unknown command '%s'\n", cmd); }
  }
}
```

- 第217行：`rl_gets()` 会阻塞等待用户输入，显示 `(nemu) ` 提示符
- 当 `cpu_exec()` 返回后，控制流回到这里，继续等待命令

## 完整的暂停流程

```
用户输入: c (继续执行)
    ↓
cmd_c() → cpu_exec(-1)
    ↓
execute() 循环执行指令
    ↓
每条指令后: check_wp()
    ↓
监视点触发: check_wp() 返回 true
    ↓
设置状态: nemu_state.state = NEMU_STOP
    ↓
break 退出 execute() 循环
    ↓
cpu_exec() 返回
    ↓
回到 sdb_mainloop()
    ↓
rl_gets() 显示 "(nemu) " 提示符，等待用户输入
    ↓
程序已暂停！✅
```

## 总结

暂停机制通过状态机实现：
1. 状态变量：`nemu_state.state` 记录当前状态
2. 状态设置：监视点触发时设置为 `NEMU_STOP`
3. 循环退出：`execute()` 中的 `break` 停止执行
4. 控制返回：`cpu_exec()` 返回后，主循环继续等待用户输入

这不是真正的“暂停CPU”，而是停止模拟器的指令执行循环，并返回到命令提示符，等待用户输入下一个命令。